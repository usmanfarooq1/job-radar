package engine

// import "testing"

// func TestMakeManager(t *testing.T) {
// 	manager := MakeManager()
// 	want := 0

// 	if len(manager.scraperTasks) != want {
// 		t.Errorf("got %d want %d", len(manager.scraperTasks), want)
// 	}
// }
